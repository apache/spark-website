# Clear the current thread's job tags.

Clear the current thread's job tags.

## Usage

``` r
clearJobTags()
```

## Note

clearJobTags since 3.5.0

## Examples

``` r
if (FALSE) { # \dontrun{
sparkR.session()
clearJobTags()
} # }
```
