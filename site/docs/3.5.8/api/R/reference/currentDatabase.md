# Returns the current default database

Returns the current default database.

## Usage

``` r
currentDatabase()
```

## Value

name of the current default database.

## Note

since 2.2.0

## Examples

``` r
if (FALSE) { # \dontrun{
sparkR.session()
currentDatabase()
} # }
```
